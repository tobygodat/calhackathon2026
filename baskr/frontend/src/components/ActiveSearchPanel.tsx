// Text input -> POST /api/search -> PaperCard list (SPEC §9).
// TODO: controlled input, call api.search(question), render hits as PaperCard.

export default function ActiveSearchPanel() {
  return (
    <section className="rounded-lg border border-neutral-800 p-4">
      <h2 className="mb-2 font-medium">Active Search</h2>
      <p className="text-sm text-neutral-500">TODO: question input + results.</p>
    </section>
  );
}
