// Renders profile items grouped by `kind` (SPEC §9). First thing the judge sees.
// TODO: fetch via api.getProfile(), group items by kind, render.

export default function LabProfilePanel() {
  return (
    <section className="rounded-lg border border-neutral-800 p-4">
      <h2 className="mb-2 font-medium">Lab Profile</h2>
      <p className="text-sm text-neutral-500">TODO: render profile items by kind.</p>
    </section>
  );
}
