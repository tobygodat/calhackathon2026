# --- Baskr backend ---
# Paper fetching is delegated to implementations/data_pipeline, which has its own
# optional keys (NCBI_API_KEY, SPRINGER_API_KEY, PIPELINE_CONTACT_EMAIL). Those can
# live here too — the pipeline reads them from the environment.

NCBI_API_KEY=             # free; raises PubMed rate limit ~3 -> ~10 req/sec
OPENAI_API_KEY=           # embeddings (text-embedding-3-small)
ANTHROPIC_API_KEY=        # reasoning LLM (Claude)
REDIS_URL=                # e.g. redis://localhost:6379  (local redis-stack for RedisVL vector search)

# --- Iris Redis Agent Memory (managed long-term memory on Redis Cloud) ---
# Provision at https://cloud.redis.io/#/agent-memory  (separate from REDIS_URL above)
AGENT_MEMORY_BASE_URL=    # e.g. https://gcp-us-east4.memory.redis.io
AGENT_MEMORY_STORE_ID=    # 32-char store id from the Cloud console
AGENT_MEMORY_API_KEY=     # store API key (bearer secret)

BASKR_LAB_ID=gut-microbiome-demo
BASKR_RELEVANCE_THRESHOLD=0.5
EMBED_MODEL=text-embedding-3-small
REASON_MODEL=claude-sonnet-4-6   # reasoning LLM (Claude); default if unset

# --- data_pipeline (optional, see implementations/data_pipeline/.env.example) ---
SPRINGER_API_KEY=
PIPELINE_CONTACT_EMAIL=
